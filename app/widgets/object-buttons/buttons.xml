<StackLayout orientation="horizontal">
    <Button text="{{ (object.attrs.acknowledgement != 0 ? 'Remove ' : '') + 'Ack' }}" isEnabled="{{ object.attrs.state != 0 }}" />
    <Button text="Check" tap="onCheckTap" />
    <Button text="Comment" />
    <Button text="Notify" />
</StackLayout>
